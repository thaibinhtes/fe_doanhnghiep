# Local development (npm run dev)
VITE_API_BASE_URL=http://localhost:8002/api

# Production build (npm run build / build:prod) — baked into dist at build time
VITE_API_BASE_URL=https://qldn.zsellers.com/api
