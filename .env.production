# Production: relative prefix only (nginx proxies /api → backend)
VITE_API_PREFIX=/api

# Production: same-origin WebSocket (nginx proxy /socket.io → socket service)
# VITE_SOCKET_URL=https://your-domain.com
